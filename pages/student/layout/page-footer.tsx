import styled from "styled-components";

const PageFooter = () => {
  return <PageFooterNaviagtion></PageFooterNaviagtion>;
};

export default PageFooter;

const PageFooterNaviagtion = styled.div`
  bottom: 0;
  position: fixed;
  width: 100%;
  height: 50px;
  background: #fff;
`;
