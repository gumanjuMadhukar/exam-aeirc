export enum LeaveActionMenu {
  APPROVE = "APPROVE",
  DECLINE = "DECLINE",
  VIEW = "VIEW",
  EDIT = "EDIT",
  DELETE = "DELETE",
}

export enum LeaveStatus {
  APPROVED = "APPROVED",
  PENDING = "PENDING",
  REJECTED = "REJECTED",
}

export enum Roles {
  ADMIN = "admin",
  EMPLOYEE = "Employee",
  SUPERADMIN = "superadmin",
  ADMINISTRATOR = "administrator",
}

export enum Leaves {
  SICK = "SICK",
  CASUAL = "CASUAL",
}
